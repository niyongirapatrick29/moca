<%- include('include/back_head.ejs') %>
  <!-- Navigation-->
<%- include('include/back_top_navigation.ejs') %>
<%- include('include/back_navigator.ejs') %>
<!-- end Navigation-->
  <!-- ============================================================== -->
  <div class="page-wrapper">
    <div class="container-fluid">
  <!-- ============================================================== -->
  <!-- Start Page Content -->
  <!-- ============================================================== -->
    <div class="row">
        <h4 class="card-title m-b-0">SLIDER IMAGES </h4>
        <a href="/admin/new_slide_image" class="btn btn-primary" style="margin-left: 65%;">INSHYA</a>
    </div>
    <hr>
    <% if(slider_data.length > 0){ %>
        <% if(errorMessage){ %>
            <h4 style="color:brown; margin-left: 10rem;"><%= errorMessage %></h4>
        <% } %>
    <div class="row el-element-overlay">
        <% for( let slide of slider_data){ %>
        <div class="col-lg-3 col-md-6">
            <div class="card">
                <div class="el-card-item">
                    <div class="el-card-avatar el-overlay-1"> 
                        <img src="/uploads/<%= slide.image %>" alt="user" />
                    </div>
                    <div class="el-card-content">
                        <h4 class="m-b-0"><%= slide.title %></h4> 
                        <span class="text-muted"><%= slide.description %></span>
                    </div>
                    <hr>
                    <div class="" style="text-align: center;">
                        <a href="/admin/slide_edit/<%=slide._id %>" class="btn btn-primary btn-sm">Edit</a>
                        <a href="/admin/slide_publish/<%= slide._id %>" class="btn btn-success btn-sm">
                            <% if(slide.slide_status === '1'){ %>
                                Unpublish
                            <% }else{ %>
                                Publish
                            <% } %>
                        </a>
                        <a href="/admin/slide_delete/<%= slide._id %>" class="btn btn-danger btn-sm">Delete</a>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
    </div>
    <% }else{%>
        <h3>No data in database</h3>
    <% } %>

 
  </div>
  <!-- ============================================================== -->
  <!-- End PAge Content -->
  <!-- ============================================================== -->
  <!-- ============================================================== -->
  <!-- Right sidebar -->
  <!-- ============================================================== -->
  <!-- .right-sidebar -->
  <!-- ============================================================== -->
  <!-- End Right sidebar -->
  <!-- ============================================================== -->
</div>
    <!-- /.container-fluid-->
    <%- include('include/back_footer.ejs') %>